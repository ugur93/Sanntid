package Event_manager

import "../driver"

const Floors := 4
var Floor_status_array [Floors]byte

func Check_inside_order(inside_order_chan chan int){
	for floor := 1; i < Floors+1: i++{
		if (Get_button_signal(BUTTON_COMMAND, floor) == 1){
			if 
	}
}
